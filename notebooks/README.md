# Notebooks

Exploratory work only. Once logic is validated here, it moves into `src/` as
proper modules with tests. Nothing in this folder is a final deliverable —
per the engineering standard, notebook-only code was the exact gap this
project exists to close.

Suggested first notebook: `01_eda_onet_oews.ipynb` — confirm both datasets
load cleanly, check the SOC crosswalk coverage (what % of O*NET occupations
find a BLS wage match), and sanity-check the skill-matching baseline on a
handful of known skill sets before committing to Phase 1.
